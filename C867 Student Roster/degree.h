#pragma once
#ifndef degree_h
#define degree_h
#include <string>

using namespace std;

// Create enum data type of DegreeProgram that has three respective degrees
enum DegreeProgram { NETWORK, SOFTWARE, SECURITY };

static const string degreeProgramStrings[] = { "NETWORK", "SOFTWARE", "SECURITY" };

#endif
